import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder smoke test.
    // Full integration tests require real Firebase config on device.
    expect(true, isTrue);
  });
}
